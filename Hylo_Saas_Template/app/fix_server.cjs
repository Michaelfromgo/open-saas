const fs = require("fs"); const path = require("path"); const serverTsPath = path.join(process.cwd(), ".wasp/build/server/src/server.ts"); console.log("Reading file:", serverTsPath); let content = fs.readFileSync(serverTsPath, "utf8"); content = content.replace("server.listen(port)", "server.listen(port, \"0.0.0.0\")"); fs.writeFileSync(serverTsPath, content); console.log("Server.ts modified to listen on 0.0.0.0");
